<h2> Product Details</h2>
Item name: <%= @item.name %><br>
Category: <%= @item_category %></br>
Price: <%= @item.price.to_f %></br>
<%= @item.image %>
<%= @item.avg_rating %>
<%= @item.description %>

Order: <%= @order %>

<%= link_to "Add to Cart", item_order_items_path(@item.id), method: :post %>
