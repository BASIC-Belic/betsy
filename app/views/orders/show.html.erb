<% @order_items.order('created_at').each do |item| %>
Item:<% x = Item.find_by(id: item.item_id) %>
<%= x.name%>





<%= form_with model: @order_item, method: :patch, :url => order_item_path(item.id) do |f| %>

<%= f.text_field :quantity_per_item %>


<%= f.submit "update quantity" %>
<%= link_to "Delete Item", order_item_path(item.id), method: :delete, data: { confirm: "are you sure you want to delete this item?" }, class: "btn btn primary" %>
<% end %>

<p> Quantity: <%= item.quantity_per_item %></p>




<br>
<br>
<br>
<% end %>
